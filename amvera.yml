meta:
  environment: jvm
  toolchain:
    name: maven
    version: "17"
build:
  args: spring-boot:repackage
  artifacts:
    target/*.jar: /
run:
  jarName: telegram-bot-0.0.1-SNAPSHOT.jar
  persistenceMount: /data
  containerPort: "8080"
